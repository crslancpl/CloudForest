# pythonscripts directory
this directory contains all the templates of all python modules in cloudforest, which are written with Python C API. There are also scripts required for application functionalities.

## init.py
*init.py : add *pythonscripts* directory to system path

### directory structure
* *entry.py* :  and call initializers.
